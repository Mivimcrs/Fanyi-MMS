# 莱·梵壹会员实时跟踪管理系统（Rust 版）— 技术文档

> 版本 v3.0.0 · 文档基于 2026-08-29 全量代码审阅编写，同日完成挂起恢复/上传残留/测试加固改造，
> 2026-08-30 增加运行期表格独占锁。`cargo test --release` 25 个测试全部通过（单元 10 + HTTP 契约 10 + 集成 5）。
> 用途：为后续开发与优化提供架构说明、数据口径、接口契约与环境指南。

---

## 1. 项目定位与技术路径

**单文件、免环境的本地会员管理系统**：Excel 工作簿是唯一数据源与唯一持久层，Rust 程序只做
「读取 → 内存计算 → 外科手术式写回」，Web 页面是操作界面。没有数据库、没有服务端状态文件（除历史版本快照）。

技术路径（Python + openpyxl 版的重写）：

| 层 | 选型 | 说明 |
|---|---|---|
| HTTP 服务 | axum 0.7 + tokio 1 | 仅绑定 127.0.0.1，单用户本地使用 |
| 读取 Excel | calamine 0.26（dates 特性） | 只读原始值，不解析样式 |
| 写入 Excel | 自研 zip + 字节级 XML 手术（`src/xlsx/`） | 核心壁垒：保真 |
| 前端 | 单文件原生 JS SPA（`static/index.html`，编译期 `include_str!` 内嵌） | 无框架、无构建步骤 |
| 桌面集成 | tray-icon + rfd + windows-sys | 托盘菜单、消息弹窗（Windows） |
| 单实例/端口 | fs2 文件锁 + 端口顺延 | 重复双击 = 拉起浏览器 |

依赖全部为纯 Rust 或纯 Rust 绑定；`profile.release` 用 `opt-level="z" + lto="fat" + strip + panic="abort"`
压体积，产物约个位数 MB。

## 2. 目录结构

```
莱·梵壹会员系统-Rust/
├── Cargo.toml / Cargo.lock     # lib = laifanyi_core，bin = laifanyi
├── src/
│   ├── main.rs                 # 启动编排（142 行）
│   ├── lib.rs                  # 常量、config.json、表格自动查找
│   ├── api.rs                  # 14 个 HTTP 路由
│   ├── store.rs                # Store：内存模型 + 全部业务写操作
│   ├── compute.rs              # 业务口径（Excel 公式的 Rust 镜像）
│   ├── model.rs                # RawVal/RawMember/RawClass + 解析工具
│   ├── snapshot.rs             # versions/ 快照与裁剪
│   ├── shell.rs                # 托盘/消息框/程序化图标
│   └── xlsx/
│       ├── read.rs             # calamine 读取两表原始行
│       ├── zipio.rs            # zip 读写、删 calcChain、强制重算
│       ├── xmlscan.rs          # 字节级 XML 扫描器（定位/模板/属性）
│       └── surgery.rs          # apply_patch 行级补丁 + 公式重编号
├── static/index.html           # 前端 SPA（1574 行，内嵌进 exe）
├── tests/roundtrip.rs          # 4 个集成测试 + fixture.xlsx
├── scripts/make_app.sh         # macOS .app 组装
├── .github/workflows/release.yml  # 双平台 CI（tag v* 触发）
└── data/                       # 开发布局：exe + 数据表放同一目录
```

## 3. 启动流程（main.rs）

1. 解析参数：`--excel <路径>`、`--port <n>`（默认 8688）、`--no-browser`。
2. 程序目录 = exe 所在目录（便携式设计，所有数据都在这里）。
3. **单实例锁**：对 `<app_dir>/.laifanyi.lock` 加排他文件锁（fs2）。拿不到锁 → 读
   `.laifanyi.port` 得到运行中端口，直接打开浏览器后退出（不产生第二进程）。
4. **端口顺延**：从指定端口起连续尝试 50 个，不杀占用进程（有意区别于 Python 版）。
   实际绑定端口写回 `.laifanyi.port`。
5. **表格定位**（三级回退）：`--excel` 参数 > 同目录精确名/前缀匹配
   `莱·梵壹会员实时跟踪管理系统*.xlsx|xlsm`（字典序取第一个）> `config.json` 的
   `excel_path`（存相对路径，支持目录迁移）。
6. 找不到表 → 消息框提示 + 前端渲染引导页（`renderSetup`），`Store` 保持 `None`；
   加载失败 → 消息框 + 退出码 1。
7. 延迟 1.5s 自动开浏览器；Windows 托盘线程（打开网页/打开数据文件夹/退出）。
8. tokio 运行 axum 服务。

Release 版 Windows 隐藏控制台（`windows_subsystem="windows"`），退出唯一途径是托盘菜单。

## 4. 数据模型：Excel 列布局（开发前必读）

工作簿含 5 张表，系统**只写**前两张，其余（实时看板、沉睡客户跟进、使用说明）纯公式驱动、
逐字节保留。两表第 1 行为表头，数据从第 2 行开始；第 2 行是「模板行」（写入时取其样式与公式）。

### 会员档案（A1:W504，`ARCHIVE_MAX_ROW=504`，容量 503 个会员）

| 列 | 字段 | 来源 |
|---|---|---|
| A1 | 会员编号 | 系统生成（见 §6 编号规则） |
| B2/C3/D4/E5 | 姓名/手机号/负责老师/会员类型 | 用户输入 |
| F6 | 卡种 | 枚举：次卡、期限卡 |
| G7/H8/I9 | 购买/开卡/有效期至 | Excel 串行日期（基准 1899-12-30） |
| J10 | 实收金额 | 用户输入 |
| K11 | 应收金额 | 用户输入（单价的分子） |
| **L12** | 尾款 | 公式 `=K2-J2` |
| M13 | 总次数 | 次卡专用 |
| **N14** | 已消耗次数 | 公式 `COUNTIFS(上课记录!B:B,A2,上课记录!J:J,"是")` |
| **O15** | 剩余次数 | 公式（次卡） |
| **P16** | 剩余天数 | 公式 `I-TODAY()`（期限卡） |
| **Q17** | 课程单价 | 公式（见 §7） |
| **R18** | 剩余课程总额 | 公式 |
| **S19** | 最后上课日期 | 公式 `MAXIFS`，无课回填开卡日 |
| **T20** | 连续未到天数 | 公式 |
| **U21** | 会员状态 | 公式状态机（见 §7） |
| V22 | 退卡 | "是"（表头 V1「退卡」由系统按需补写） |
| W23 | 备注 | 用户输入 |

### 上课记录（A1:N199，公式预填到 199 行；`RECORDS_MAX_ROW=1001` 可越界插入）

| 列 | 字段 | 来源 |
|---|---|---|
| A1 | 记录编号 | 系统 = 现有最大序号 + 1 |
| B2 | 会员编号 | 用户选择 |
| **C3/F6/G7/H8** | 姓名/老师/类型/卡种 | 公式 `XLOOKUP(B, 会员档案!A:A, …)` |
| D4/E5 | 上课日期/时间 | 串行日期 / 时间分数（0..1） |
| **I9** | 课程单价 | 公式 `VLOOKUP(B, 会员档案!A:Q, 17)` |
| **J10** | 是否有效扣卡 | 公式：卡种 ∈ {次卡,期限卡} → "是" |
| **K11** | 消耗数量 | 公式 `J="是" → 1` |
| L12 | 课程名称 / M13 效果图("1") / N14 备注 | 用户输入 |

**关键约束**：系统对加粗列（公式列）只写一次模板公式（`ensure_formulas`，已有公式则不碰），
其余全部依赖 Excel 打开时重算。任何绕过 surgery 直接改值的做法都会破坏口径。

## 5. 架构与数据流

```
                 ┌────────────────────────────────────────────┐
                 │ Store（Mutex<Option<Store>>，全局单实例）      │
  xlsx 文件 ──读──▶ entries: Vec<Entry>（zip 全部件原始字节）       │
                 │ members/classes: Raw*（calamine 读出的原始行） │
                 │ tpl_archive/tpl_class: 第2行模板(样式+公式)    │
                 │ doc_bytes: 已重算打包好的最终 zip              │
                 └───────┬────────────────────────────────────┘
   GET /api/data         │ POST 变更
                         ▼
   compute::compute()  patch_sheet()：apply_patch 改目标表 XML
   （业务口径镜像）        → finalize_and_write：删 calcChain + fullCalcOnLoad + 重打包
                         → rebuild()：重读内存（members/classes 与磁盘逻辑一致）
                         → save()：先 snapshot 旧文件到 versions/，再 tmp+rename 原子落盘
```

要点：

- **每次变更 = 全量重打包**：`patch_sheet` 只改目标表的 XML 字节，但 `finalize_and_write`
  会重新压缩所有部件。其余部件字节内容不变（测试 `surgery_fidelity` 逐字节断言）。
- **save() 可能"挂起"**：落盘遇 `PermissionDenied`（Excel/WPS 正开着文件）→ `pending=true`，
  变更保留在内存，前端左下角显示「待保存 + 重试」按钮；`/api/save` 重试，`/api/reload`
  在 pending 时拒绝（防丢改动）。**失败的同时把内存工作簿落盘为挂起工件**
  （`app_dir/.laifanyi.pending.xlsx` + `.laifanyi.pending.json` 元数据，记录目标表格/ts/操作/说明）：
  进程被杀后重启，`Store::load` 检测到工件且目标匹配 → `meta.pending_recovery` 暴露给前端，
  弹窗提供 `/api/pending/recover`（原子覆盖表格并重载）/ `/api/pending/discard`（放弃）。
  单槽位语义：保存成功、恢复、丢弃、换表（`/api/file`）都会清理工件。
- **运行期独占锁（Windows）**：`Store` 持有一个 `share_mode=0` 的常驻打开句柄（`acquire_file_lock`），
  系统运行期间外部程序（Excel/WPS/任何工具）对表格的打开/读写都被内核以共享冲突拒绝；
  托盘退出或换表即解锁。系统自身的磁盘操作（快照/落盘/重载/恢复版本）统一走
  `disk_unlocked`（临时释放句柄 → 操作 → 重新独占）。两个已知边界：
  ① 现代 Windows 上 `std::fs::rename` 走 POSIX 语义，可替换持有旧句柄的文件（旧句柄继续有效），
  这正是系统自身保存能越过锁窗口的原因，也意味着"工具级整文件替换"无法防——但 Excel/WPS
  的编辑路径必须先打开文件，已被完全封锁；② macOS 无强制共享锁语义，暂不实现（advisory 锁对
  Excel 无效）。启动时若表格被短暂占用（如杀软扫描、Excel 已打开），锁定失败仅告警不退出——
  每次 `/api/data` 轮询与后续磁盘操作都会持续重试补锁（自愈）。
- **快照时机**：非 pending 的每次 `save()` 先把**磁盘上的旧文件**拷入 `versions/`
  （`时间戳_序号_动作slug.xlsx` + 同名 .json 元数据），上限 `MAX_VERSIONS=100`，超出按 mtime
  裁最旧。恢复版本前会自动再做一次快照，因此恢复本身可撤销；挂起恢复覆盖的磁盘状态
  也已在此前的首次 save 时快照，不会丢失。

## 6. 编号规则（gen_member_id）

- 新客户：扫全部会员编号取 `M<数字>` 的最大值 +1 → `M042`。
- 老客户新办卡（前端传 `base_member_id`，或按手机号匹配到已有卡）：沿用基础号，
  在 `-N` 后缀上取最大 +1 → `M001-2`。
- 会员不存在编号冲突检测（依赖生成规则天然唯一）。

## 7. 业务口径（compute.rs，Excel U/Q/R 列的 Rust 镜像）

`compute(today, members, classes) -> (MemberOut[], ClassOut[], Dashboard)`，today 显式传入以便测试。

**课程单价 Q**：次卡 = 应收K / 总次数M（K≠0 且 M>0）；期限卡 = 应收K / (有效期至I − 开卡H)（I>H）。
不满足条件 → None（前端显示 —，不计入营收）。

**剩余课程金额 R** = 单价 × 剩余次数/天数（退卡恒为 0，前端显示 —）。

**最后上课日期 S**：该会员全部上课记录的最大日期（注意：不筛 J="是"，与 Excel 公式
`MAXIFS(...,J,"是")` 存在已知口径差，Python 版即如此，测试按此基准）；无课回填开卡日期。

**连续未到天数 T** = today − 最后上课日期。

**状态机 U**（优先级从上到下）：
1. 卡耗尽（次卡 O=0 或期限卡 P=0）→ 开卡日为空 = **待开卡**，否则 **已到期**；
2. 未到 ≥ 10 天 → **沉睡客户**；≥ 7 天 → **预警客户**；否则 **正常**；
3. V 列退卡 → 一律 **退卡**（覆盖以上所有）。

**Dashboard 口径**：
- 营收/耗课类：耗课 = 有效上课记录（卡种合法）单价之和；办卡收入 = 会员实收金额 J 之和；
  「今日/本月」按购买日期、上课日期过滤（月前缀匹配）。
- 大屏的「区间营收/耗课」由**前端**对全量数据重新聚合（`rangeStats/rangeIncome`），
  不走后端——改区间口径只需动前端。
- 累计负债 = Σ 未退卡会员剩余课程金额。
- 有效期客户 = 未退卡且 expiry_date ≥ today（字符串比较，日期格式恒为 YYYY-MM-DD 所以安全）。
- 金额保留：后端仅 `balance_due` 做 round2，其余原样输出；前端负责千分位显示。

## 8. HTTP API 契约（api.rs，与 Python 版逐字兼容）

全部 JSON；错误统一 `{"ok":false,"error":"..."}`，业务错误 400、系统错误 500。

| 方法 | 路径 | 作用 | 备注 |
|---|---|---|---|
| GET | `/`、`/index.html` | 内嵌前端 | Cache-Control: no-cache |
| GET | `/api/data` | 全量数据 `{members, classes, dashboard, meta}` | 无文件时 `{no_file:true, reason}` |
| GET | `/api/versions` | 版本列表（新→旧） | 前端有缓存，变更后失效 |
| GET | `/api/versions/download?file=` | 下载快照 | 取 basename 防穿越，仅 .xlsx |
| POST | `/api/file` | `{name, data_b64}` 上传切换表格 | 先 calamine 校验必须含两业务表；旧表自动快照 |
| POST | `/api/members` | 新增会员 | 校验：姓名/卡种必填，次卡须总次数>0，期限卡须有效期 |
| POST | `/api/members/update` | `{id, fields{}}` 编辑/退卡/恢复 | refund:true→标记退卡（自动补 V1 表头），false→恢复 |
| POST | `/api/classes` | 添加上课记录 | 会员必须存在；时间缺省取当前时刻 |
| POST | `/api/classes/delete` | `{row}` 删除记录 | 清空该行 A,B,D,E,L,M,N（保留公式列） |
| POST | `/api/versions/backup` | `{note}` 手动备份 | |
| POST | `/api/versions/restore` | `{file}` 恢复版本 | 恢复前自动快照当前状态 |
| POST | `/api/save` | 重试挂起保存 | |
| POST | `/api/reload` | 放弃内存变更从磁盘重读 | pending 时拒绝 |
| POST | `/api/pending/recover` | 用挂起工件覆盖表格并重载 | 无挂起/目标不符 → 400；表格仍被占用 → 400 |
| POST | `/api/pending/discard` | 丢弃挂起变更 | 幂等；未加载表格时也能按程序目录清理 |

成功响应除业务数据外都带 `meta`（`saved/pending_reason/last_saved/容量`），前端据此渲染保存状态。

## 9. Excel 写入引擎（src/xlsx/）——本项目的核心资产

前提假设（xmlscan.rs 头注释）：输入恒为 Excel/openpyxl 产出的规范 XML，因此用**字节级精确扫描**
替代通用 XML 解析器，换来「未触及部分逐字节保留」——切片器、条件格式、数据校验、
数组公式、其他工作表全部无损（openpyxl 会丢切片器，这是 Rust 版的核心优势）。

- **xmlscan.rs**：`next_tag`（跳过注释/CDATA、引号感知）、`attr_value`（实体反转义）、
  `sheet_data_span`、`row_templates`（提取模板行每列的 `s=样式` 与 `<f>公式`）、
  `sheet_paths`（workbook.xml + rels 解析表名→部件路径，支持中文表名）、`fix_dimension`。
- **surgery.rs**：`apply_patch` 按行号合并写指令（Write 覆盖 / Clear 删除 / EnsureFormula
  仅当无公式时补 / insert_if_absent 按行序插队新建），新单元格写成 `inlineStr`
  （不碰 sharedStrings.xml）或纯 `<v>` 数值；`renumber_formula` 把模板公式里的相对引用
  `K2→K504` 重编号（`$K$2`、`AB20`、`A2AB` 等边界有单测覆盖）。
- **zipio.rs**：`finalize_and_write` 删除 `xl/calcChain.xml`（同步清理 Content_Types 与
  workbook rels）并在 workbook.xml 注入 `fullCalcOnLoad="1"`，保证 TODAY()/状态列
  打开即重算。
- **read.rs**：calamine 读 1..MAX_ROW；编号列为空的行跳过（洞可复用，
  `first_empty_row` 找第一个空行写入）。

**写入路径的已知边界**（改动前须知）：
- 行内单元格重排为按列号升序（BTreeMap）——对机器生成的 XML 无影响，不要手工制造乱序行再交给系统。
- 文本一律 inlineStr，若原单元格是 shared string 类型，重写后类型变化（openpyxl/Excel 均可正常读，往返测试通过）。
- `escape()` 只处理 `& < >`，因为文本写在 `<t>` 内容区而非属性区，够用；若未来往属性区写动态文本需扩展。

## 10. 前端（static/index.html）

- 原生 JS SPA，无框架无构建：四个视图（数据大屏/客户管理/上课记录/历史版本）+ 抽屉详情 + 各类弹窗。
- 全局状态 `S`；`refresh()` 拉 `/api/data` 后 `render()`；变更统一走 `afterMutation()`
  （刷新 + 版本缓存失效 + 保存状态 + toast）。
- 大屏支持自定义统计区间（起止日期 + 预设），KPI 卡片可点击下钻到会员/记录明细弹窗。
- 手写 SVG 图表：环形图、柱线组合趋势图（>62 天自动按月聚合）、横向条形老师业绩。
- 明暗双主题（CSS 变量，白日模式 = 鼠尾草绿，已做 AA 对比度），localStorage 持久化。
- 60s 轮询 `/api/data`（有弹窗/抽屉打开时跳过），实现"多人同时看、Excel 手改后自动同步"的近似实时。

## 11. 本机构建指南（已验证可跑通）

本机为 GNU 工具链 + MinGW-w64（无 Visual Studio），且项目路径含中文，MinGW ld/dlltool
无法处理中文路径。**已验证的可用命令**（Git Bash）：

```bash
cd "D:\UserData\Desktop\莱·梵壹会员系统-Rust"
CARGO_TARGET_DIR="/d/AppData/.zcode/workspace/default/lfy-target" \
PATH="/c/Users/Mivimcrs/.cargo/bin:/c/Users/Mivimcrs/winlibs/mingw64/bin:$PATH" \
cargo test --release
```

两个环境变量缺一不可：缺 `CARGO_TARGET_DIR` → 中文路径链接失败；缺 winlibs PATH →
`error calling dlltool 'dlltool.exe': program not found`（编译 windows-sys 时）。

CI（`.github/workflows/release.yml`）不受此限制：Windows 用 MSVC，macOS 用 Xcode，
打 tag `v*` 或手动触发，产出 Windows zip + macOS universal2 .app（ad-hoc 签名，
正式分发需在 secrets 配 Developer ID 并替换）。

### 11.1 无 Mac 交叉编译 macOS（2026-08-29 已验证跑通）

工具：`rustup target add aarch64-apple-darwin x86_64-apple-darwin` +
`pip install ziglang`（zig 0.16）+ `cargo install cargo-zigbuild`（安装需 MinGW PATH，同上）。
构建（ziglang 路径需加入 PATH）：

```bash
export PATH="/c/Users/Mivimcrs/AppData/Local/Programs/Python/Python313/Lib/site-packages/ziglang:$PATH"
cargo zigbuild --release --target aarch64-apple-darwin   # Apple Silicon
cargo zigbuild --release --target x86_64-apple-darwin    # Intel
```

关键前提——**macOS 目标不能链接任何 Apple 框架**（zig 只带 libSystem.tbd，无 SDK）：
- `rfd`/`tray-icon` 已收窄到 `[target.'cfg(windows)'.dependencies]`（macOS 代码路径本就不用它们）；
- chrono 已去 `clock` 特性（其 iana-time-zone 依赖会在 macOS 链接 CoreFoundation），
  `Local::now()` 改为 `model::local_now()`：unix 用 `libc::localtime_r`、Windows 用
  `GetLocalTime`（`chrono` 只保留日期解析/运算）。改动后 `cargo tree --target
  aarch64-apple-darwin -i core-foundation-sys` 应报"无匹配"。
- 构建时"xcrun/SDK not found"警告为良性；arm64 产物由 zig 自动 ad-hoc 签名。
- Windows 侧回归测试 23/23 通过后该方法已固化；lipo 合并 universal2 仍需 macOS（走 CI）。

## 12. 测试

`cargo test`：单元测试（model 列号/日期解析、surgery 公式重编号、store 挂起工件恢复/丢弃/
目标不匹配、Windows 独占锁阻塞外部打开、锁穿越 save 周期、first_empty_row）+ 集成测试：

| 测试 | 覆盖 |
|---|---|
| `tests/roundtrip.rs::compute_matches_excel` | 5 个固定会员的口径黄金对照（状态/单价/负债/营收），基准值来自 Python 版与 Excel 实算 |
| `::write_path_roundtrip` | 新增→加课→删课→退卡→落盘→重读全链路 |
| `::surgery_fidelity` | 除目标表与 workbook 外全部 zip 部件逐字节一致；条件格式/校验/公式仍在 |
| `::surgery_insert_beyond_prefill` | 199 行预填外（250 行）插入 + 公式重编号 |
| `::surgery_insert_at_capacity_row` | 容量边界：第 1001 行插入 + 公式重编号 + dimension 扩展 |
| `tests/api_tests.rs`（10 个） | HTTP 契约：`/api/data` 有/无文件、会员四类必填校验 400、新增→落盘→编辑→再落盘、上课增删（不存在会员/无效日期/重复删除/非法行号 400）、`/api/file`（非法扩展名/非法 base64/非法 zip → 400，合法上传成功且无 .tmp 残留、config.json 更新）、版本备份/列表/下载/路径穿越 404/恢复（含"恢复前自动备份"）、`/api/save`+`/api/reload`（未加载表格 400）、`/api/pending/*` 契约与全链路 HTTP 恢复 |

挂起恢复链路的单元测试在 `src/store.rs` 的 `#[cfg(test)] mod tests`（`write_pending_artifacts`
为 pub，集成测试 `pending_recover_via_http` 也直接用它模拟"保存失败"场景）。

fixture.xlsx 即真实数据表副本（含 5 名示例会员）。

## 13. 待办与优化建议（按优先级）

### 功能待办（README 里程碑遗留）
1. **M4 余项**：✅ 应用图标 .ico + VERSIONINFO 已嵌入（v3.0.1，build.rs + winresource，见 防误报指南.md）；
   macOS Developer ID 签名 + 公证仍待（或改走 SignPath Foundation 免费签名，见下）。
2. **M5**：macOS 真机 UAT；macOS 托盘（需 NSApp 主线程事件循环，shell.rs 已预留 cfg 分支）。
3. 上课记录 199→1000 行扩容验证 —— ✅ 已补第 1001 行容量边界测试（`surgery_insert_at_capacity_row`）。

### 杀软误报缓解（2026-08-30）
- ✅ exe 嵌入品牌图标 + VERSIONINFO（FileDescription 等，最大静态启发式因子）。
- ✅ 分发与日常运行改用 CI 的 MSVC 产物（GNU 构建误报权重更高）。
- 🚧 免费签名：LICENSE(MIT) 已加；用户将仓库改公开并申请 SignPath Foundation（OV 级、免费、
  开源项目验证制）后，把签名接入 release.yml —— 治本。
- 每版发售后按 `防误报指南.md` 提交 Defender/Avast/360 误报申诉。

### 代码级优化（2026-08-29 已完成一批，标注 ✅）
4. ✅ **挂起变更落盘**：save 失败时写 `.laifanyi.pending.xlsx` + 元数据，重启后网页提示
   一键恢复/丢弃（`/api/pending/recover|discard`）；换表/保存成功自动清理（单槽位）。
5. ✅ **api_file 失败残留**：改为内存级 `from_bytes` 校验，全部通过后才临时文件+rename
   原子落盘，失败零残留、不影响当前表格。
6. ✅ **测试缺口（主体）**：新增 `tests/api_tests.rs`（10 个 HTTP 契约测试，tower oneshot）
   + 挂起恢复单元测试 + 容量边界测试。仍缺：xmlscan 对畸形输入的模糊测试（低优先级）。
7. **依赖升级注意**：axum 0.7→0.8 有路由语法破坏性变化（`/:id` → `/{id}`）；calamine/zip
   升级后需重跑保真测试。当前全部钉在主版本内，无急迫性。
8. ✅ **first_empty_row O(n²)→HashSet**。
9. 体验项（低优先级）：数据大屏区间口径在前端聚合，若要与其他端共享逻辑可把 `range stats` 下沉到
   `/api/data`；前端 60s 轮询可换 SSE（单用户场景收益有限）。

### 安全边界（现状即设计，勿破坏）
- 服务只绑 127.0.0.1、无鉴权——保持不对外网监听；如需局域网访问必须先加鉴权。
- 文件上传/版本恢复均取 basename 防路径穿越；新增文件类接口时沿用此模式。
