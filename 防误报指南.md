# 杀软误报防护指南（免费方案）

> 本程序未签名且行为特征（本地服务、自动开浏览器、独占锁定数据文件）容易被启发式引擎误判。
> 本指南是每次发版后的固定动作清单 + 背景说明。代码签名接入后（见文末），误报可基本根治。

## 一、为什么会被报毒

| 因素 | 说明 | 状态 |
|---|---|---|
| 无版本信息资源 | 文件属性描述/公司/版本全空，是未签名 exe 最大的静态启发式命中点 | ✅ v3.0.1 已修复（build.rs 嵌入图标 + VERSIONINFO） |
| MinGW/GNU 构建 | 杀软对 GNU 链接的未签名 exe 权重更高（恶意软件惯用工具链） | ✅ 分发与日常使用改用 CI 的 MSVC 产物 |
| 行为特征 | 隐藏控制台 + 本地监听端口 + 拉起浏览器 + 文件锁 | 核心设计，无法移除 |
| 全新哈希零信誉 | 每次发版都是杀软没见过的新文件 | 靠提交误报申诉积累 |
| 无代码签名 | 根因 | 🚧 SignPath 免费签名接入中 |

## 二、每次发版后的固定动作（免费）

以 v3.0.1 为例，先取文件指纹（把 <exe路径> 换成 Release 解压出的 laifanyi.exe）：

```
certutil -hashfile laifanyi.exe SHA256
```

按实际报毒的杀软提交（都免费）：

1. **微软 Defender**（报毒首选处理）
   打开 https://www.microsoft.com/wdsi/filesubmission → 选「Software」→ 上传 laifanyi.exe →
   填写公司/产品信息与说明（本地会员管理工具，误报）→ 提交后数小时到数天内微软云端除名，全网 Defender 用户同步生效。
2. **Avast / AVG**
   打开 https://www.avast.com/submit-a-sample → 选 **False positive** → File → 上传 exe →
   填邮箱等待分析结果（约 48 小时）。AVG 同源引擎，也可走 https://www.avg.com/report-false-positive 。
3. **360**（国内用户可选）
   打开 https://open.soft.360.cn/report.php → 按提示提交文件与邮箱（1-2 个工作日）；
   申诉邮箱备用：opensoft@360.cn（标题：【软件管家】+ 问题提要）。
4. **腾讯电脑管家**：无在线平台，到其官方论坛发帖反馈误报。

> 提示：提交时用**实际分发的那个 exe**（哈希必须与用户拿到的完全一致）。每发一个新版本都要重新提交一次。

## 三、本机即时缓解（开发者/已知用户电脑）

### Defender 排除目录（管理员 PowerShell 或 设置→隐私和安全性→Windows 安全中心→病毒和威胁防护→排除项）
```
Add-MpPreference -ExclusionPath "D:\UserData\Desktop\莱·梵壹会员系统-Rust\data"
Add-MpPreference -ExclusionPath "D:\AppData\.zcode\workspace\default\lfy-target2"
```

### Avast：病毒防护 → 例外 → 添加例外 → 文件/文件夹 → 选 data 目录

排除只影响本机，不能替代第二节的全网申诉。

## 四、治本：免费代码签名（进行中）

- 仓库公开 + MIT 协议后，申请 **SignPath Foundation** 免费签名（https://signpath.org ）：
  面向开源项目的 OV 级代码签名证书，不验证个人身份、验证"二进制由公开源码构建"，审核需数日。
- 审核通过后把签名步骤接入 `.github/workflows/release.yml`（SignPath.io API 签署 CI 产物），
  此后 Release 的 exe 自带有效签名——SmartScreen 警告消失，各杀软误报概率降到最低。

## 五、期望管理

签名接入前，每个新版本的未签名 exe 仍可能被个别引擎的机器学习模型偶发命中——
「资源 + MSVC 构建 + 每版提交误报」能把概率压到最低，但没有任何免费手段能 100% 杜绝。
接入签名后此问题基本消失。
